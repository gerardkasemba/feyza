# =============================================================================
# Feyza – Environment variables
# Copy to .env.local and fill in values. Do not commit .env.local.
# =============================================================================

# -----------------------------------------------------------------------------
# Public (safe to expose in browser)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=https://feyza.app
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_PAYPAL_CLIENT_ID=your-paypal-client-id

# -----------------------------------------------------------------------------
# Secrets (server-only; never expose)
# -----------------------------------------------------------------------------
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Cron jobs (Vercel cron calls with this in Authorization: Bearer <CRON_SECRET>)
CRON_SECRET=your-cron-secret

# Admin / backfill routes (Bearer token or cookie)
ADMIN_SECRET=your-admin-secret

# Optional: contact for payment proof / support
ADMIN_EMAIL=admin@feyza.app

# SMTP (sending email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Feyza" <hello@feyza.app>

# Plaid (bank linking)
PLAID_ENV=sandbox
PLAID_CLIENT_ID=
PLAID_SECRET=

# Dwolla (ACH)
DWOLLA_APP_KEY=
DWOLLA_APP_SECRET=
DWOLLA_ENV=sandbox
DWOLLA_WEBHOOK_SECRET=

# PayPal (server-side)
PAYPAL_SECRET=
# Production: set PAYPAL_API_URL or use NODE_ENV (production uses live URL)

# Partner API (optional)
FEYZA_PARTNER_SECRET=

# Agent (disbursement / support)
AGENT_DEFAULT_PASSWORD=

# -----------------------------------------------------------------------------
# Observability (optional)
# -----------------------------------------------------------------------------
# Sentry: set to enable error reporting in production
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
